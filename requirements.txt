# requirements.txt
# 설치: pip install -r requirements.txt

# ── 데이터 수집 ────────────────────────────────────────────────
# HTTP 요청
requests==2.31.0

# 데이터 처리
pandas==2.2.0

# XML 파싱 (NTIS API 응답)
lxml==4.9.4

# ── KG Builder ────────────────────────────────────────────────
# LightRAG (KG 기반 RAG)
lightrag-hku>=1.3.0

# LLM 호출 (OpenAI 호환 엔드포인트)
openai>=1.54.0

# 한국어 로컬 임베딩
sentence-transformers>=3.3.0
torch>=2.0.0

# Neo4j 연동
neo4j>=5.0.0

# 시각화
pyvis>=0.3.2
networkx>=3.2.0

# ── 백엔드 API ─────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# ── 공통 ──────────────────────────────────────────────────────
numpy>=1.26.0
python-dotenv>=1.0.0
